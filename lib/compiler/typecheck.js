var ASTVisitor = require("./visitor");

// Check semantic properties local to a vertex or fragment shader.

Typechecker = function(shader) {

};

Typechecker.prototype = {
    constructor: Typechecker,
}

module.exports = Typechecker;
